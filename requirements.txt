# Core dependencies
openai>=1.0.0
python-dotenv>=1.0.0
rich>=13.0.0
pydantic>=2.0.0
pydantic-settings>=2.0.0

# Voice - Speech-to-text (Phase 3)
openai-whisper>=20230918
# pyaudio>=0.2.13  # Optional: for microphone input

# Voice - Text-to-speech
# kokoro>=0.8.0  # Optional: local neural TTS (install separately)
# piper-tts>=1.0.0  # Optional: alternative TTS
soundfile>=0.12.0

# Memory (Phase 2)
# sqlite3 is built-in

# Utilities
aiohttp>=3.8.0

# Ollama (local LLM support)
# Install Ollama separately: curl -fsSL https://ollama.com/install.sh | sh